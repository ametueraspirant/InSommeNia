/// @description spawn moobs
var random_x = random(room_width);
var random_y = random(room_height);

if(point_distance(random_x, random_y, obj_player.x, obj_player.y) > 200){
	if(instance_number(obj_enemy_oculo < 10)){
		
	}
}